<% @user ||= current_user %>
<div class="stats mt-6 grid grid-cols-2 gap-2">
  <div>
    <strong id="following" class="stat">
      <%= @user.following.count %>
    </strong>

    following
  </div>
  <div>
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    followers
  </div>
</div>
